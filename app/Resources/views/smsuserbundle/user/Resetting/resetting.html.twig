{% extends "@SNSBundlesUser/layout.html.twig" %}

{% block body %}  
    <div class="md-card-content large-padding" >
        <a href="{{ path ('login') }}" class="uk-position-top-right uk-close uk-margin-right uk-margin-top "></a>
        <h2 class="heading_a uk-margin-large-bottom">Reset password</h2>
        {{ form_start(form ) }}
            {{ form_widget(form) }}
            <div class="uk-margin-medium-top">
                <button type="submit" class="md-btn md-btn-primary md-btn-block">Reset password</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}   